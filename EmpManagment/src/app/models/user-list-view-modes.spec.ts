import { UserListViewModes } from './user-list-view-modes';

describe('UserListViewModes', () => {
  it('should create an instance', () => {
    expect(new UserListViewModes()).toBeTruthy();
  });
});
