import { UserRolesViewModel } from './user-roles-view-model';

describe('UserRolesViewModel', () => {
  it('should create an instance', () => {
    expect(new UserRolesViewModel()).toBeTruthy();
  });
});
