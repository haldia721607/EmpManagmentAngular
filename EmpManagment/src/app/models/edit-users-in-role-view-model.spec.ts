import { EditUsersInRoleViewModel } from './edit-users-in-role-view-model';

describe('EditUsersInRoleViewModel', () => {
  it('should create an instance', () => {
    expect(new EditUsersInRoleViewModel()).toBeTruthy();
  });
});
