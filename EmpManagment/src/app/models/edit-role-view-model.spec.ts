import { EditRoleViewModel } from './edit-role-view-model';

describe('EditRoleViewModel', () => {
  it('should create an instance', () => {
    expect(new EditRoleViewModel()).toBeTruthy();
  });
});
